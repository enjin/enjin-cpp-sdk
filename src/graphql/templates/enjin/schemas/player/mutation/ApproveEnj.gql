#namespace enjin.sdk.player.ApproveEnj
#import enjin.sdk.shared.Transaction

#arg value BigInt!
#arg send Boolean true

mutation {
    result: ApproveEnj(
        value: $value,
        send: $send
    ) {
        ...Transaction
    }
}
