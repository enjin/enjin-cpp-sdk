#namespace enjin.sdk.player.MeltAsset
#import enjin.sdk.shared.Transaction

#arg melts [MeltInput!]!
#arg send Boolean true

mutation {
    result: MeltAsset(
        melts: $melts,
        send: $send
    ) {
        ...Transaction
    }
}
