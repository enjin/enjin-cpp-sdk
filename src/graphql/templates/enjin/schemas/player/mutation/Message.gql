#namespace enjin.sdk.player.Message
#import enjin.sdk.shared.Transaction

#arg message String!
#arg send Boolean true

mutation {
    result: Message(
        message: $message,
        send: $send
    ) {
        ...Transaction
    }
}
